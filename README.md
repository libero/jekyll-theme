# Libero Jekyll Theme

## Getting started
To use this theme, add `remote_theme: libero/jekyll-theme` to your `_config.yml` in your project repository.

## Running locally
Run `make docs` to view this theme locally.
